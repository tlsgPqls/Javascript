// rest 매개변수

sum(1, 2, 3, 4, 5);
function sum(...nums) {
  console.log(nums);
}

// 내부적으로 array

sum1(1, 2, 3, 4, 5);
function sum1(num1, nums, ...num2) {
  console.log(num1);
  console.log(num2);
  console.log(nums);
}
//rest 매개변수는 반드시 마지막에 나와야한다
