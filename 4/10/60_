//즉시 실패 promise - reject, catch
Promise.reject("error").catch(console.log);

function checkAge(age) {
  if (age < 18) {
    return Promise.reject("미성년자");
  }
  return Promise.resolve("pass");
}
checkAge(15).catch(console.log); //then 이 붙을수도 있음
